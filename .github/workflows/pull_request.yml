name: PR Pipeline

on:
  pull_request:
    branches:
      - master
    types:
      - opened

jobs:
  agent-analysis:
    runs-on: ubuntu-latest
    steps:
        - uses: VitoorFranca/pr-analysis@v001
          with:
            greeting: 'Hello World'
